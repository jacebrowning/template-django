[Make]

cli = make
version = GNU Make

[Python]

cli = python
version = 3.9

[Poetry]

cli = poetry
version = 1
optional = true
message = The version check is currently returning an error on CircleCI.

[PostgreSQL]

cli = psql
version = 13 || 14
optional = true
message = This is required for development. Binary absent on CircleCI.

[direnv]

cli = direnv
cli_version_arg = --help
version = direnv v2
optional = true
message = Needed to manage environment variables.

[Heroku CLI]

cli = heroku
version = heroku/7
optional = true
message = Need to debug production: https://devcenter.heroku.com/articles/heroku-cli

[Firefox]

cli = firefox
version = 66 || 70
optional = true
message = Needed to run system tests using Firefox. Binary is absent on macOS.

[geckodriver]

cli = geckodriver
version = 0.29
optional = true
message = Needed to run system tests using Firefox.

[terminal-notifier]

cli = terminal-notifier
version = 1.8 || 2
optional = true
message = Needed to send you macOS notifications.

[Graphviz]

cli = dot
cli_version_arg = -V
version = 2
optional = true
message = Needed to generate UML diagrams: brew install graphviz
