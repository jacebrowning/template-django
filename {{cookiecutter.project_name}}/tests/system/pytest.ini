[pytest]

DJANGO_SETTINGS_MODULE = config.settings.test

addopts =
    --strict-markers
    --pdbcls=tests.debugger:Debugger
    --last-failed

    --verbose
    -r sxX

    --fail-on-template-vars

cache_dir = .cache

markers =
